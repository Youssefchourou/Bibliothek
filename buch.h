#ifndef BUCH_H
#define BUCH_H
#include"iostream"
#include<vector>

using namespace std;

class Buch
{
/*private:
    string nachname;
    string Titel;

    int Nummer;
    double Preis;
    static int counter;
    bool aktiv=true;
    bool ausgeliehen=false;

public:
    Buch(string nachname,string Titel,double Preis);
    ~Buch();
    int getNummer() const;

    bool getAktiv() const;
    void setAktiv(bool newAktiv);
    string getNachname() const;
    string getTitel() const;
    bool getAusgeliehen() const;
    void setAusgeliehen(bool newAusgeliehen);
    double getPreis() const;*/
};

#endif // BUCH_H
